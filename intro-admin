<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>INTRO ADMIN</title>
<style>
*{ margin:0; padding:0; box-sizing:border-box; }

body{
  background:#0a0a0a;
  font-family:Arial;
  color:white;
  min-height:100vh;
}

nav{
  height:70px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:0 24px;
  border-bottom:1px solid rgba(255,255,255,.1);
  position:sticky;
  top:0;
  background:#0a0a0a;
  z-index:100;
}

.navLeft{ display:flex; align-items:center; gap:16px; }
.logo{ font-size:16px; letter-spacing:4px; font-weight:bold; }
.badge{
  font-size:11px;
  background:#7c5cff;
  padding:4px 10px;
  border-radius:20px;
}

.navRight{ display:flex; gap:10px; align-items:center; }

.navBtn{
  padding:8px 18px;
  border:none;
  border-radius:10px;
  background:#1a1a1a;
  color:white;
  font-size:13px;
  cursor:pointer;
  letter-spacing:1px;
  transition:.2s;
  text-decoration:none;
  display:inline-block;
}
.navBtn:hover{ background:#2a2a2a; }
.navBtnPurple{ background:#7c5cff; }
.navBtnPurple:hover{ background:#6a4dee; }

/* AUTH GATE */
#authGate{
  position:fixed;
  inset:0;
  background:#0a0a0a;
  z-index:999;
  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;
  gap:16px;
}

#authGate h2{ letter-spacing:4px; font-size:20px; }
#authGate p{ opacity:.4; font-size:13px; letter-spacing:1px; }

#keyInput{
  padding:14px 20px;
  border-radius:12px;
  border:none;
  background:#1a1a1a;
  color:white;
  font-size:16px;
  letter-spacing:4px;
  text-align:center;
  width:280px;
  margin-top:8px;
  outline:none;
  border:1px solid transparent;
  transition:.2s;
}
#keyInput:focus{ border-color:rgba(124,92,255,.5); }

.btnPurple{
  padding:13px 32px;
  border:none;
  border-radius:12px;
  background:#7c5cff;
  color:white;
  font-size:14px;
  letter-spacing:2px;
  cursor:pointer;
  transition:.2s;
}
.btnPurple:hover{ background:#6a4dee; }
.btnPurple:disabled{ opacity:.4; cursor:not-allowed; }

#authError{ color:#ff5c5c; font-size:13px; display:none; }

/* MAIN */
#mainContent{ display:none; }

.pageHeader{
  padding:40px 32px 24px;
  display:flex;
  justify-content:space-between;
  align-items:flex-end;
  flex-wrap:wrap;
  gap:16px;
}

.pageTitle{ font-size:30px; letter-spacing:3px; }
.pageSubtitle{ font-size:13px; opacity:.4; margin-top:6px; letter-spacing:1px; }

.info{
  background:#111;
  border:1px solid rgba(255,255,255,.06);
  border-radius:14px;
  padding:16px 20px;
  margin:0 32px 24px;
  font-size:13px;
  opacity:.7;
  line-height:1.8;
}
.info strong{ color:#7c5cff; opacity:1; }

/* UPLOAD AREA */
.uploadSection{
  margin:0 32px 32px;
  background:#111;
  border:1px solid rgba(255,255,255,.06);
  border-radius:16px;
  padding:28px;
}

.uploadSection h3{
  font-size:15px;
  letter-spacing:2px;
  margin-bottom:20px;
  opacity:.8;
}

.dropZone{
  border:2px dashed rgba(255,255,255,.18);
  border-radius:14px;
  padding:40px 20px;
  text-align:center;
  cursor:pointer;
  transition:.3s;
  margin-bottom:14px;
  font-size:15px;
}
.dropZone:hover{ border-color:rgba(255,255,255,.45); background:rgba(255,255,255,.02); }
.dropZone.over{ border-color:#7c5cff; background:rgba(124,92,255,.07); }
.dropZone p{ font-size:12px; opacity:.4; margin-top:8px; }

.inputField{
  width:100%;
  padding:13px 16px;
  border-radius:10px;
  border:none;
  background:#1a1a1a;
  color:white;
  font-size:14px;
  margin-bottom:14px;
  outline:none;
  border:1px solid transparent;
  transition:.2s;
}
.inputField:focus{ border-color:rgba(124,92,255,.4); }

.progressBar{
  width:100%;
  height:4px;
  background:rgba(255,255,255,.08);
  border-radius:99px;
  overflow:hidden;
  margin-bottom:8px;
  display:none;
}
.progressFill{
  height:100%;
  background:#7c5cff;
  width:0%;
  transition:.4s;
}

.statusText{ font-size:12px; opacity:.5; min-height:18px; margin-bottom:12px; }

.selectedFile{
  display:flex;
  align-items:center;
  gap:12px;
  background:#1a1a1a;
  border-radius:10px;
  padding:12px 16px;
  margin-bottom:14px;
  display:none;
}
.selectedFile span{ flex:1; font-size:13px; opacity:.7; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
.selectedFile video{
  width:60px; height:40px;
  border-radius:6px;
  object-fit:cover;
  flex-shrink:0;
}

/* GRID DE VÍDEOS */
.videosSection{
  margin:0 32px 60px;
}

.videosSection h3{
  font-size:15px;
  letter-spacing:2px;
  margin-bottom:20px;
  opacity:.8;
}

.videosGrid{
  display:grid;
  grid-template-columns:repeat(auto-fill, minmax(220px,1fr));
  gap:16px;
}

.videoCard{
  background:#111;
  border-radius:14px;
  overflow:hidden;
  border:1px solid rgba(255,255,255,.06);
  transition:.2s;
}

.videoCard:hover{ border-color:rgba(255,255,255,.15); }

.videoCard video{
  width:100%;
  height:140px;
  object-fit:cover;
  display:block;
}

.videoCardInfo{
  padding:12px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:8px;
}

.videoCardName{
  font-size:13px;
  opacity:.7;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
  flex:1;
}

.videoCardBtns{ display:flex; gap:6px; flex-shrink:0; }

.btnRemove{
  padding:6px 12px;
  border:none;
  border-radius:8px;
  background:rgba(200,50,50,.25);
  color:#ff7070;
  font-size:12px;
  cursor:pointer;
  transition:.2s;
  letter-spacing:1px;
}
.btnRemove:hover{ background:rgba(200,50,50,.5); }

.featuredBadge{
  font-size:10px;
  background:#7c5cff;
  padding:3px 8px;
  border-radius:20px;
  letter-spacing:1px;
}

.emptyState{
  text-align:center;
  padding:60px 20px;
  opacity:.3;
  grid-column:1/-1;
}
.emptyState .icon{ font-size:48px; margin-bottom:14px; }
.emptyState p{ font-size:14px; letter-spacing:1px; }
</style>
</head>
<body>

<!-- AUTH GATE -->
<div id="authGate">
  <h2>🔐 ADMIN</h2>
  <p>ACESSO RESTRITO — INTRO VÍDEOS</p>
  <input id="keyInput" type="password" placeholder="••••••" maxlength="10"
    onkeydown="if(event.key==='Enter') checkKey()">
  <button class="btnPurple" onclick="checkKey()">ENTRAR</button>
  <div id="authError">Chave incorreta.</div>
</div>

<!-- MAIN -->
<div id="mainContent">

  <nav>
    <div class="navLeft">
      <div class="logo">INTRO ADMIN</div>
      <span class="badge">ADMIN</span>
    </div>
    <div class="navRight">
      <a href="intro.html" class="navBtn navBtnPurple">▶ VER INTRO</a>
      <a href="home.html" class="navBtn">🎵 HOME</a>
    </div>
  </nav>

  <div class="pageHeader">
    <div>
      <div class="pageTitle">VÍDEOS DO INTRO</div>
      <div class="pageSubtitle">GERENCIE OS CLIPES QUE APARECEM NA TELA DE ABERTURA</div>
    </div>
  </div>

  <div class="info">
    O intro usa até <strong>11 slots</strong> de vídeo. Se você adicionar menos, eles serão repetidos para preencher todos os quadros.<br>
    O <strong>primeiro vídeo</strong> da lista (ou o marcado como destaque ⭐) também aparece no <strong>fundo desfocado</strong>.<br>
    Formatos aceitos: <strong>MP4, WebM, MOV</strong>. Recomendado: até 50MB por vídeo.
  </div>

  <!-- UPLOAD -->
  <div class="uploadSection">
    <h3>➕ ADICIONAR VÍDEO</h3>

    <div class="dropZone" id="videoDropZone" onclick="document.getElementById('videoFileInput').click()">
      🎬 Arraste o vídeo aqui ou clique para selecionar
      <p>MP4, WebM ou MOV — recomendado até 50MB</p>
    </div>
    <input type="file" id="videoFileInput" accept="video/*" style="display:none">

    <div class="selectedFile" id="selectedFile">
      <video id="selectedPreview" muted autoplay loop playsinline></video>
      <span id="selectedFileName">—</span>
    </div>

    <input class="inputField" id="videoLabel" placeholder="Nome / descrição (opcional)  ex: Butter MV Dance">

    <div class="progressBar" id="uploadProgress"><div class="progressFill" id="uploadFill"></div></div>
    <div class="statusText" id="uploadStatus"></div>

    <button class="btnPurple" id="uploadBtn" onclick="uploadVideo()">ENVIAR VÍDEO</button>
  </div>

  <!-- LISTA -->
  <div class="videosSection">
    <h3>📋 VÍDEOS ADICIONADOS</h3>
    <div class="videosGrid" id="videosGrid">
      <div class="emptyState">
        <div class="icon">🎬</div>
        <p>Carregando...</p>
      </div>
    </div>
  </div>

</div>

<script type="module">

import { initializeApp } from "https://www.gstatic.com/firebasejs/12.14.0/firebase-app.js"
import { getDatabase, ref, onValue, push, remove, update } from "https://www.gstatic.com/firebasejs/12.14.0/firebase-database.js"

const firebaseConfig = {
  apiKey: "AIzaSyDFShfAw6VJ51hjka27gUebd9fna6o4yGw",
  authDomain: "bts-archive-63f8b.firebaseapp.com",
  databaseURL: "https://bts-archive-63f8b-default-rtdb.firebaseio.com",
  projectId: "bts-archive-63f8b",
  storageBucket: "bts-archive-63f8b.firebasestorage.app",
  messagingSenderId: "995910359395",
  appId: "1:995910359395:web:faa0858e7a44cf101f9b56"
}

const CLOUDINARY_CLOUD = "dlcg5zzes"
const CLOUDINARY_PRESET = "bts_upload"

const app = initializeApp(firebaseConfig)
const db = getDatabase(app)
const videosRef = ref(db, "introVideos")

let videos = []
let selectedFile = null

// ─── AUTH ────────────────────────────────────────────────────
window.checkKey = () => {
  const key = document.getElementById("keyInput").value
  if(key === "12345"){
    document.getElementById("authGate").style.display = "none"
    document.getElementById("mainContent").style.display = "block"
  } else {
    document.getElementById("authError").style.display = "block"
    document.getElementById("keyInput").value = ""
  }
}

// ─── FIREBASE ────────────────────────────────────────────────
onValue(videosRef, (snapshot) => {
  videos = []
  if(snapshot.exists()){
    snapshot.forEach(child => {
      videos.push({ id: child.key, ...child.val() })
    })
  }
  renderGrid()
})

// ─── RENDER ──────────────────────────────────────────────────
function renderGrid(){
  const grid = document.getElementById("videosGrid")
  if(videos.length === 0){
    grid.innerHTML = `
      <div class="emptyState">
        <div class="icon">🎬</div>
        <p>Nenhum vídeo ainda. Adicione o primeiro acima.</p>
      </div>`
    return
  }

  grid.innerHTML = videos.map((v, i) => `
    <div class="videoCard">
      <video src="${v.url}" muted autoplay loop playsinline></video>
      <div class="videoCardInfo">
        <span class="videoCardName">${v.label || 'Vídeo ' + (i+1)}</span>
        <div class="videoCardBtns">
          ${v.featured ? '<span class="featuredBadge">⭐ FUNDO</span>' : `<button onclick="setFeatured('${v.id}')" style="padding:6px 10px;border:none;border-radius:8px;background:#1a1a1a;color:rgba(255,255,255,.5);font-size:11px;cursor:pointer;letter-spacing:1px;">⭐</button>`}
          <button class="btnRemove" onclick="removeVideo('${v.id}')">✕</button>
        </div>
      </div>
    </div>
  `).join("")
}

// ─── FILE SELECT ─────────────────────────────────────────────
const dropZone = document.getElementById("videoDropZone")
const fileInput = document.getElementById("videoFileInput")

dropZone.addEventListener("dragover", (e) => { e.preventDefault(); dropZone.classList.add("over") })
dropZone.addEventListener("dragleave", () => dropZone.classList.remove("over"))
dropZone.addEventListener("drop", (e) => {
  e.preventDefault()
  dropZone.classList.remove("over")
  const file = e.dataTransfer.files[0]
  if(file && file.type.startsWith("video/")) handleFile(file)
  else alert("Selecione um arquivo de vídeo!")
})
fileInput.addEventListener("change", () => {
  if(fileInput.files[0]) handleFile(fileInput.files[0])
})

function handleFile(file){
  selectedFile = file
  const preview = document.getElementById("selectedPreview")
  preview.src = URL.createObjectURL(file)
  document.getElementById("selectedFileName").innerText = file.name
  document.getElementById("selectedFile").style.display = "flex"
}

// ─── UPLOAD ──────────────────────────────────────────────────
window.uploadVideo = async () => {
  if(!selectedFile){ alert("Selecione um vídeo primeiro!"); return }

  const btn = document.getElementById("uploadBtn")
  btn.disabled = true

  const progress = document.getElementById("uploadProgress")
  const fill = document.getElementById("uploadFill")
  const status = document.getElementById("uploadStatus")
  const label = document.getElementById("videoLabel").value.trim()

  progress.style.display = "block"
  fill.style.width = "0%"
  status.innerText = "Enviando para o Cloudinary..."

  try {
    const url = await uploadToCloudinary(selectedFile, p => {
      fill.style.width = p + "%"
      status.innerText = `Enviando... ${p}%`
    })

    await push(videosRef, {
      url,
      label: label || selectedFile.name.replace(/\.[^.]+$/, ""),
      featured: videos.length === 0 // primeiro vídeo vira destaque automaticamente
    })

    fill.style.width = "100%"
    status.innerText = "✅ Vídeo adicionado!"

    // Reset form
    selectedFile = null
    document.getElementById("selectedFile").style.display = "none"
    document.getElementById("selectedPreview").src = ""
    document.getElementById("videoLabel").value = ""
    document.getElementById("selectedFileName").innerText = "—"
    fileInput.value = ""
    setTimeout(()=>{
      progress.style.display = "none"
      status.innerText = ""
    }, 2000)

  } catch(err){
    status.innerText = "❌ Erro no upload. Tente novamente."
    console.error(err)
  }

  btn.disabled = false
}

async function uploadToCloudinary(file, onProgress){
  return new Promise((resolve, reject) => {
    const formData = new FormData()
    formData.append("file", file)
    formData.append("upload_preset", CLOUDINARY_PRESET)

    const xhr = new XMLHttpRequest()
    xhr.open("POST", `https://api.cloudinary.com/v1_1/${CLOUDINARY_CLOUD}/video/upload`)
    xhr.upload.onprogress = (e) => {
      if(e.lengthComputable) onProgress(Math.round(e.loaded / e.total * 100))
    }
    xhr.onload = () => {
      const res = JSON.parse(xhr.responseText)
      if(res.secure_url) resolve(res.secure_url)
      else reject(res)
    }
    xhr.onerror = reject
    xhr.send(formData)
  })
}

// ─── REMOVE ──────────────────────────────────────────────────
window.removeVideo = async (id) => {
  if(!confirm("Remover este vídeo do intro?")) return
  await remove(ref(db, "introVideos/" + id))
}

// ─── FEATURED (vídeo de fundo) ───────────────────────────────
window.setFeatured = async (id) => {
  // Tira featured de todos, coloca no selecionado
  const updates = {}
  videos.forEach(v => {
    updates[`introVideos/${v.id}/featured`] = v.id === id
  })
  const { update: dbUpdate } = await import("https://www.gstatic.com/firebasejs/12.14.0/firebase-database.js")
  await dbUpdate(ref(db), updates)
}

</script>
</body>
</html>
